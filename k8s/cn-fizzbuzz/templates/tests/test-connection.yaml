apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cn-fizzbuzz.fullname" . }}-test-connection"
  labels:
{{ include "cn-fizzbuzz.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cn-fizzbuzz.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
